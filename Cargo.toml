[workspace]
members = ["bootloader", "kernel", "modules/common", "modules/memory", "project_build"]
resolver = "3"

[workspace.dependencies]

[profile.dev]
panic = "abort"

[profile.release]
panic = "abort"
