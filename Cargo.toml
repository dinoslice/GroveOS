[workspace]
members = ["bootloader", "kernel", "modules/common", "modules/fbcon", "modules/memory", "project_build"]
resolver = "3"

[workspace.dependencies]

[profile.dev]
panic = "abort"

[profile.release]
panic = "abort"
